import requests
from pdfplumber import PDFPlumberLoader

def call_chat_only_completion_ocr(ocr_prompt: str, system_prompt: str, user_prompt: str, 
                                model: str = "gemma-3-4b-it", max_token: int = 4000, 
                                temperature: float = 0.1) -> str:
    # [Keep your existing function implementation]
    pass

def load_pdf(file_path: str) -> str:
    # [Keep your existing function implementation]
    pass